// FILE: src/app/Cycle/login/page.tsx

import LoginClient from './LoginClient'

export const metadata = { title: 'Inloggen — Cycle Companion' }

export default function LoginPage() {
  return <LoginClient />
}
